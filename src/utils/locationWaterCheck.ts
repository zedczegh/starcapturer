
/**
 * Check if a location is on water (e.g. oceans, lakes)
 * This is a simplified implementation that could be replaced with a more accurate check
 * in a real application.
 */
export function isWaterLocation(latitude: number, longitude: number): boolean {
  // This is a mock implementation that always returns false
  // In a real application, you might use a geographical database or API
  // to determine if a location is on water
  return false;
}
